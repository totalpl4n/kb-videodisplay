@echo off
echo This program will install OpenCV and NumPy using Pip. Make sure you've installed Pip before running this program, which can be done with pip-installation.bat.
pause

pip install opencv-python

pip install numpy
